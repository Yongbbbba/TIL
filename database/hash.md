```mssql
USE Northwind;



-- Hash(해시) 조인

-- 인덱스가 없을 때 위력을 발휘한다.

-- 해시테이블을 만드는데 용량을 차지한다. 공간을 내주고 속도를 얻는 것이다. 

-- 동일한 값을 가지면 동일한 bucket

-- 하지만 동일한 bucket이어도 동일한 값은 아닐 수 있음. 반대쪽으로는 통행이 안된다.

-- key로 value는 찾아도 value로 key를 찾을 수는 없음. 



-- 작은 쪽을 Hashtable로 !



-- 오늘의 결론



-- Hash 조인

-- 1) 정렬이 필요하지 않다. -> 데이터가 너무 많아서 Merge가 부담스러울 때, hash가 대안이 될 수 있음

-- 2) 인덱스 유무에 영향을 받지 않는다. ***** 

-- NL/Merge에 비해 확실한 장점 ! 

-- Hashtable 만드는 비용을 무시하면 안된다. 공간을 내주고 속도를 얻는다.

-- 드물게 조회되는 것이 아니라, 수행빈도가 많으면 결국 Index를 만들어 관리하는 것이 유리하다. 

-- 3) 랜덤 액세스 위주로 수행되지 않는다.(랜덤 액세스란, RID를 이용해 룩업테이블을 액세스하거나, 키값으로 클러스터 인덱스 페이지를 찾아보는 것)

-- 4) 데이터가 적은 쪽을 hashtable로 만드는 것이 유리하다.
```

